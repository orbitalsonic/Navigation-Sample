package com.orbitalsonic.navigationsample.presentation.base.activities.enums

enum class InsetsMode {
    EDGE_TO_EDGE,      // No padding
    TOP,               // Status bar only
    BOTTOM,            // Nav bar / IME only
    TOP_BOTTOM         // Both
}